# Tests

- `unit/` pour la logique isolée des règles du correcteur (`corrector/rules/*`).
- `integration/` pour le chemin complet (`corrector.correct_docx`), avec Microsoft Word réel.

```bash
python -m pytest
```
